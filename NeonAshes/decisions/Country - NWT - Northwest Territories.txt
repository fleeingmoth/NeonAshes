political_decisions = {
	norman_wells_oil = {
		news = yes
		news_desc_long = "norman_wells_oil_NEWS_LONG"
		news_desc_medium = "norman_wells_oil_NEWS_MEDIUM"
		news_desc_short = "norman_wells_oil_NEWS_SHORT"
		picture = norman_wells_smallpic
		potential = {
			owns = 10				# We own Inuvik (incl. Norman Wells)
			NOT = { 10 = { trade_goods = oil } }	# Not pumping oil
		}
		allow = {
			war = no				# War makes it harder
			fossil_fuel_power = 1			# We can work with oil
		}
		effect = {
			prestige = 1
			10 = { trade_goods = oil }		# Drill baby drill!
			treasury = 10000			# Small investment of 10,000 Kilodollars
		}
		ai_will_do = {
			factor = 1				# It's pretty cool
			modifier = {				# Environmentalists oppose the idea
				factor = 0
				ruling_party_ideology = green
			}
		}
	}
	resolute_rocket_launch_site = {
		news = yes
		news_desc_long = "resolute_rocket_launch_site_NEWS_LONG"
		news_desc_medium = "resolute_rocket_launch_site_NEWS_MEDIUM"
		news_desc_short = "resolute_rocket_launch_site_NEWS_SHORT"
		picture = spaceport_resolute_smallpic
		potential = {
			owns = 45		# Iqaluit - Resolute
			is_core = 45		# Only build in core territory
			suborbital_flight = 1	# Rocket science
			45 = {
				NOT = {		# Already built
					OR = {
						has_province_modifier = rocket_launch_site_location
						has_province_modifier = space_center_location
						has_province_modifier = spaceport_location
					}
				}
			}
			NOT = { has_country_flag = hide_rocket_sites } 	# Decluttering
		}
		allow = {
			owns = 45				# We have Resolute
			is_core = 45				# Only build in core territory
			aerospace_industry = 1			# We can build rockets
			has_country_modifier = arctic_surveyor	# Challenging Arctic conditions
			war = no				# War makes things harder
			money = 100000				# Must be able to afford it
			NOT = {					# We just built one
				OR = {
					has_country_modifier = recently_built_launch_site
					has_country_modifier = recently_built_space_center
					has_country_modifier = recently_built_spaceport
				}
			}
		}
		effect = {
			45 = {
				add_province_modifier = {
					name = rocket_launch_site_location
					duration = -1
				}
			}
			add_country_modifier = {
				name = recently_built_launch_site
				duration = 180
			}
			prestige = 5	# It makes the news
			money = -100000	# It costs a lot
		}
		ai_will_do = {
			factor = 0.01
			modifier = {
				factor = 0	# We already have one somewhere else
				OR = {
					any_core = { has_province_modifier = rocket_launch_site_location }
					any_core = { has_province_modifier = space_center_location }
					any_core = { has_province_modifier = spaceport_location }
				}
			}
			modifier = {
				factor = 10	# NWT prefers to build here
				tag = NWT
			}
		}
	}
	resolute_space_center = {
		news = yes
		news_desc_long = "resolute_space_center_NEWS_LONG"
		news_desc_medium = "resolute_space_center_NEWS_MEDIUM"
		news_desc_short = "resolute_space_center_NEWS_SHORT"
		picture = spaceport_resolute_smallpic
		potential = {
			owns = 45							# Iqaluit - Resolute
			is_core = 45							# Only build in core territory
			45 = { has_province_modifier = rocket_launch_site_location }	# We have a rocket launch site
			NOT = { has_country_flag = hide_rocket_sites } 			# Decluttering
		}
		allow = {
			owns = 45					# We have Resolute
			is_core = 45					# Only build in core territory
			orbital_flight = 1				# We have the technology
			has_country_modifier = arctic_researcher	# Challenging Arctic conditions
			war = no					# War makes things harder
			money = 200000					# Must be able to afford it
			NOT = {						# We just built one
				OR = {
					has_country_modifier = recently_built_launch_site
					has_country_modifier = recently_built_space_center
					has_country_modifier = recently_built_spaceport
				}
			}
		}
		effect = {
			45 = {
				remove_province_modifier = rocket_launch_site_location
				add_province_modifier = {
					name = space_center_location	# Upgrades
					duration = -1
				}
			}
			add_country_modifier = {
				name = recently_built_space_center
				duration = 365
			}
			prestige = 10	# It is renowned across the world
			money = -200000	# It costs a lot
		}
		ai_will_do = {
			factor = 0.01
			modifier = {
				factor = 0	# We already have one somewhere else
				OR = {
					any_core = { has_province_modifier = space_center_location }
					any_core = { has_province_modifier = spaceport_location }
				}
			}
			modifier = {
				factor = 10	# NWT prefers to build here
				tag = NWT
			}
		}
	}
	resolute_spaceport = {
		news = yes
		news_desc_long = "resolute_spaceport_NEWS_LONG"
		news_desc_medium = "resolute_spaceport_NEWS_MEDIUM"
		news_desc_short = "resolute_spaceport_NEWS_SHORT"
		picture = spaceport_resolute_smallpic
		potential = {
			owns = 45						# Iqaluit - Resolute
			is_core = 45						# Only build in core territory
			45 = { has_province_modifier = space_center_location }	# We have a space center
			NOT = { has_country_flag = hide_rocket_sites } 		# Decluttering
		}
		allow = {
			owns = 45				# We have Resolute
			is_core = 45				# Only build in core territory
			interplanetary_flight = 1		# We have the technology
			fly_by_wire = 1				# We have good avionics
			has_country_modifier = arctic_exploiter	# Challenging Arctic conditions
			war = no				# War makes things harder
			money = 500000				# Must be able to afford it
			NOT = {					# We just built one
				OR = {
					has_country_modifier = recently_built_launch_site
					has_country_modifier = recently_built_space_center
					has_country_modifier = recently_built_spaceport
				}
			}
		}
		effect = {
			45 = {
				remove_province_modifier = space_center_location
				add_province_modifier = {
					name = spaceport_location	# Upgrades
					duration = -1
				}
			}
			add_country_modifier = {
				name = recently_built_spaceport
				duration = 730
			}
			prestige = 15	# It is renowned across the world
			money = -500000	# It costs a lot
		}
		ai_will_do = {
			factor = 0.01
			modifier = {
				factor = 0	# We already have one somewhere else
				any_core = { has_province_modifier = spaceport_location }
			}
			modifier = {
				factor = 10	# NWT prefers to build here
				tag = NWT
			}
		}
	}
}
