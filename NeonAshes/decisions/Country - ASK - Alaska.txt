political_decisions = {
	alaska_intro = {
		news = no
		picture = walter_cronkite_smallpic
		potential = {
			tag = ASK							# We are Alaska
			NOT = { has_country_modifier = ask_national_spirit }	# We don't have the national modifier
		}
		allow = {								# Redundant, but it tells you what you need to know.
			tag = ASK							# We are Alaska
			NOT = { has_country_modifier = ask_national_spirit }	# We don't have the national modifier
		}
		effect = {
			country_event = {
				id = 108
				days = 0
			}
		}
		ai_will_do = { factor = 1 }
	}
	kodiak_rocket_launch_site = {
		news = yes
		news_desc_long = "kodiak_rocket_launch_site_NEWS_LONG"
		news_desc_medium = "kodiak_rocket_launch_site_NEWS_MEDIUM"
		news_desc_short = "kodiak_rocket_launch_site_NEWS_SHORT"
		picture = spaceport_kodiak_smallpic
		potential = {
			owns = 3		# Anchorage - Kodiak Island
			is_core = 3		# Only build in core territory
			suborbital_flight = 1	# Rocket science
			3 = {
				NOT = {		# Already built
					OR = {
						has_province_modifier = rocket_launch_site_location
						has_province_modifier = space_center_location
						has_province_modifier = spaceport_location
					}
				}
			}
			NOT = { has_country_flag = hide_rocket_sites } 	# Decluttering
		}
		allow = {
			owns = 3		# We have Kodiak Island
			is_core = 3		# Only build in core territory
			aerospace_industry = 1	# We can build rockets
			war = no		# War makes things harder
			money = 100000		# Must be able to afford it
			NOT = {			# We just built one
				OR = {
					has_country_modifier = recently_built_launch_site
					has_country_modifier = recently_built_space_center
					has_country_modifier = recently_built_spaceport
				}
			}
		}
		effect = {
			3 = {
				add_province_modifier = {
					name = rocket_launch_site_location
					duration = -1
				}
			}
			add_country_modifier = {
				name = recently_built_launch_site
				duration = 180
			}
			prestige = 5	# It makes the news
			money = -100000	# It costs a lot
		}
		ai_will_do = {
			factor = 0.01
			modifier = {
				factor = 0	# We already have one somewhere else
				OR = {
					any_core = { has_province_modifier = rocket_launch_site_location }
					any_core = { has_province_modifier = space_center_location }
					any_core = { has_province_modifier = spaceport_location }
				}
			}
			modifier = {
				factor = 10	# Alaska prefers to build here
				tag = ASK
			}
		}
	}
	kodiak_space_center = {
		news = yes
		news_desc_long = "kodiak_space_center_NEWS_LONG"
		news_desc_medium = "kodiak_space_center_NEWS_MEDIUM"
		news_desc_short = "kodiak_space_center_NEWS_SHORT"
		picture = spaceport_kodiak_smallpic
		potential = {
			owns = 3							# Anchorage - Kodiak Island
			is_core = 3							# Only build in core territory
			3 = { has_province_modifier = rocket_launch_site_location }	# We have a rocket launch site
			NOT = { has_country_flag = hide_rocket_sites } 			# Decluttering
		}
		allow = {
			owns = 3			# We have Kodiak Island
			is_core = 3			# Only build in core territory
			orbital_flight = 1		# We have the technology
			modern_transportation = 1	# We can move rockets
			war = no			# War makes things harder
			money = 200000			# Must be able to afford it
			NOT = {				# We just built one
				OR = {
					has_country_modifier = recently_built_launch_site
					has_country_modifier = recently_built_space_center
					has_country_modifier = recently_built_spaceport
				}
			}
		}
		effect = {
			3 = {
				remove_province_modifier = rocket_launch_site_location
				add_province_modifier = {
					name = space_center_location	# Upgrades
					duration = -1
				}
			}
			add_country_modifier = {
				name = recently_built_space_center
				duration = 365
			}
			prestige = 10	# It is renowned across the world
			money = -200000	# It costs a lot
		}
		ai_will_do = {
			factor = 0.01
			modifier = {
				factor = 0	# We already have one somewhere else
				OR = {
					any_core = { has_province_modifier = space_center_location }
					any_core = { has_province_modifier = spaceport_location }
				}
			}
			modifier = {
				factor = 10	# Alaska prefers to build here
				tag = ASK
			}
		}
	}
	kodiak_spaceport = {
		news = yes
		news_desc_long = "kodiak_spaceport_NEWS_LONG"
		news_desc_medium = "kodiak_spaceport_NEWS_MEDIUM"
		news_desc_short = "kodiak_spaceport_NEWS_SHORT"
		picture = spaceport_kodiak_smallpic
		potential = {
			owns = 3						# Anchorage - Kodiak Island
			is_core = 3						# Only build in core territory
			3 = { has_province_modifier = space_center_location }	# We have a space center
			NOT = { has_country_flag = hide_rocket_sites } 		# Decluttering
		}
		allow = {
			owns = 3			# We have Kodiak Island
			is_core = 3			# Only build in core territory
			interplanetary_flight = 1	# We have the technology
			fly_by_wire = 1			# We have good avionics
			war = no			# War makes things harder
			money = 500000			# Must be able to afford it
			NOT = {				# We just built one
				OR = {
					has_country_modifier = recently_built_launch_site
					has_country_modifier = recently_built_space_center
					has_country_modifier = recently_built_spaceport
				}
			}
		}
		effect = {
			3 = {
				remove_province_modifier = space_center_location
				add_province_modifier = {
					name = spaceport_location	# Upgrades
					duration = -1
				}
			}
			add_country_modifier = {
				name = recently_built_spaceport
				duration = 730
			}
			prestige = 15	# It is renowned across the world
			money = -500000	# It costs a lot
		}
		ai_will_do = {
			factor = 0.01
			modifier = {
				factor = 0	# We already have one somewhere else
				any_core = { has_province_modifier = spaceport_location }
			}
			modifier = {
				factor = 10	# Alaska prefers to build here
				tag = ASK
			}
		}
	}
}
